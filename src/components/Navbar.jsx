import { useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { HiArrowUpRight, HiBars3, HiXMark } from 'react-icons/hi2'
const links = ['Home', 'Services', 'Results', 'Pricing', 'Contact']
export default function Navbar() { const [open, setOpen] = useState(false); const close = () => setOpen(false); return <header className="nav-wrap"><nav className="nav container" aria-label="Main navigation"><a className="logo" href="#home" onClick={close}><i>✦</i> NOVAPEAK</a><div className="nav-links">{links.map(x => <a key={x} href={`#${x.toLowerCase()}`}>{x}</a>)}</div><a href="#contact" className="nav-cta">Book strategy call <HiArrowUpRight /></a><button className="menu" aria-label={open ? 'Close menu' : 'Open menu'} aria-expanded={open} onClick={() => setOpen(!open)}>{open ? <HiXMark /> : <HiBars3 />}</button><AnimatePresence>{open && <motion.div className="mobile-nav" initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -12 }}>{links.map(x => <a key={x} href={`#${x.toLowerCase()}`} onClick={close}>{x}</a>)}<a href="#contact" onClick={close}>Book strategy call <HiArrowUpRight /></a></motion.div>}</AnimatePresence></nav></header> }
